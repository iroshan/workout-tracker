{
  "name": "workout-tracker-offline",
  "version": "1.2.0",
  "private": true,
  "description": "Offline local-first workout tracker packaged for Android with Capacitor",
  "scripts": {
    "android:init": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "build:debug": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:release": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.9.3"
  }
}
